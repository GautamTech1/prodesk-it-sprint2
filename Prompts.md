Write index.html using tailwind cdn for a cash-flow tracker. include inputs for salary and expenses  and display areas for total expenses and balance.

Write strict vanilla js to calculate 'balance  salary - total expenses  render expenses dynamically in a list. use number to prevent string concatenation.

Add js validation to block empty inputs and negative numbers with a simple alert.

Update js to save salary and expenses using  retrieve them on page load using  so data persists.

Add a delete button to each expense that removes it from the array updates localstorage and recalculates the balance instantly.

Inject chart.js  and render a pie chart showing 'balance vs expenses must use destroy before re-rendering to prevent duplication bugs.

if remaining balance falls below 10% of salary, turn the balance text red and show a warning banner.

Integrate js pdf to download the expense report. use  api (inr to usd) to add a currency toggle button
